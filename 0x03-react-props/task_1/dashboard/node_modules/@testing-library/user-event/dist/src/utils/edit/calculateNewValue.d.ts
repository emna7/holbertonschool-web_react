export declare function calculateNewValue(newEntry: string, element: HTMLElement, value?: string | null, selectionRange?: {
    selectionStart: number | null;
    selectionEnd: number | null;
}): {
    newValue: string;
    newSelectionStart: number;
};
