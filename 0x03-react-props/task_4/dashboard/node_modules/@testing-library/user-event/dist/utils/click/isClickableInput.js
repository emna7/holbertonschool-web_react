"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isClickableInput = isClickableInput;

var _isInstanceOfElement = require("../misc/isInstanceOfElement");

const CLICKABLE_INPUT_TYPES = ['button', 'color', 'file', 'image', 'reset', 'submit'];

function isClickableInput(element) {
  return element.tagName === 'BUTTON' || (0, _isInstanceOfElement.isInstanceOfElement)(element, 'HTMLInputElement') && CLICKABLE_INPUT_TYPES.includes(element.type);
}